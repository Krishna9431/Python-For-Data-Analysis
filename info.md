- Install Python 3.13 for virtual environment

`conda create -p venv python=3.13` (according as per requirements, install python version)

- Activate venv: `conda activate vevn/`

- When run in venv, run : 
    
    ![[Error will received if we not installed ipykernal]](<Screenshot/ErrorImage for not installinf ipykernal.png>)

    - Run: `pip install ipykernel`
    - What is 'ipykernel' ? 
        - It is a package -> It is a kernel for Jupyter notebook. It is used to run python code in Jupyter notebook.

- Git add:
    - `git add .`
    - `git commit -m "Initial commit"`
    - `git push -u origin main`
